

function Login() {
  // const { theme, user } = useContext(GlobalContext); // Ensure GlobalContext is available
  return (
    <div>
      <h1>Login</h1>
    </div>
  );

  

}

export default Login;